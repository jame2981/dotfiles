[ids]
*

[main]
# Right side bottom row swap: [RSuper] [RAlt] → [RAlt] [RSuper]
rightmeta = rightalt
rightalt  = overload(control, rightmeta)

# Left diamond key: overload — held with combo = Ctrl, tapped alone = Super
leftmeta = overload(control, leftmeta)
